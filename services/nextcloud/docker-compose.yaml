services:
  db:
    image: mariadb:10.6
    container_name: nextcloud-db
    dns:
      - 192.168.0.19
    command: --transaction-isolation=READ-COMMITTED --binlog-format=ROW
    restart: always
    volumes:
      - ./nextcloud_db:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root_password_alterar
      - MYSQL_PASSWORD=user_password_alterar
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
    networks:
      - homelab_network

  redis:
    image: redis:7.2-alpine
    container_name: nextcloud-redis
    dns:
      - 192.168.0.19
    restart: always
    networks:
      - homelab_network

  nextcloud:
    image: nextcloud:30.0.6
    container_name: nextcloud
    dns:
      - 192.168.0.19
    restart: always
    ports:
      - 8080:80
    volumes:
      - ./data/nextcloud_data:/var/www/html
      - /Volumes/Elements/homelab/nextcloud/nextcloud_data:/var/www/html/data
    environment:
      - MYSQL_PASSWORD=user_password_alterar
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
      - MYSQL_HOST=db
      - REDIS_HOST=redis
      - APACHE_SERVER_NAME=nextcloud.momos
    depends_on:
      - db
      - redis
    networks:
      - homelab_network

networks:
  homelab_network:
    external: true